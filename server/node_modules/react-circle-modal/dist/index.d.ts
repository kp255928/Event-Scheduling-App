import React from 'react';
declare type RefContent = {
    menuStatus: boolean;
    closeMenu: () => void;
    openMenu: () => void;
};
interface Props {
    backgroundColor: string;
    children: (onClick: (event: React.MouseEvent) => void) => React.ReactNode;
    toogleComponent: (onClick: (event: React.MouseEvent) => void, status: boolean) => React.ReactNode;
    offsetX?: number;
    offsetY?: number;
}
export declare const ReactCircleModal: React.ForwardRefExoticComponent<Props & React.RefAttributes<RefContent | null>>;
export default ReactCircleModal;
